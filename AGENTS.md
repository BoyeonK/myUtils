# Agent Instructions

이 저장소는 다른 프로젝트에서 재사용하는 C++17 런타임/유틸리티 정적 라이브러리다.

## 기본 원칙

* Correctness를 micro-optimization보다 우선한다.
* 측정 근거가 없는 성능 최적화를 먼저 추가하지 않는다.
* 이미 동작하는 단순한 구조를 추측만으로 복잡하게 만들지 않는다.
* 요청받지 않은 대규모 리팩터링이나 범위 확장을 함께 수행하지 않는다.
* 문서에서 확인한 사실, 코드에서 확인한 사실, 자신의 추론을 구분한다.
* 기존 설계와 코드가 어긋나는 경우 임의로 한쪽을 맞추지 말고 먼저 차이를 보고한다.

## 문서 역할

문서마다 역할이 다르므로 중복해서 새로운 규칙을 만들지 않는다.

* `design/*.md`

  * 여러 파일과 타입에 걸친 현재 실행 규약과 설계
* `design/adr/`

  * 중요한 설계 결정을 내린 이유와 당시 판단
* `design/OPEN_QUESTIONS.md`

  * 아직 무엇을 할지 결정되지 않은 문제
* `progress.md`

  * 무엇을 할지는 이미 정해졌지만 아직 수행되지 않은 작업
* 코드 주석

  * 해당 구현 바로 옆에 있어야 의미가 유지되는 국소적인 invariant와 제약

컴포넌트를 수정하기 전에 해당 컴포넌트의 설계 문서와 관련 ADR을 먼저 확인한다.

## ADR

Accepted ADR은 새로운 근거나 명시적인 재검토 요청 없이 다시 논의하지 않는다.

기존 결정에 문제가 있다고 판단되면 ADR을 조용히 덮어쓰지 않는다.

다음 중 어떤 경우인지 구분한다.

* 구현이 기존 결정과 어긋남
* 기존 결정의 전제가 더 이상 성립하지 않음
* 새로운 측정 결과가 재검토 조건을 충족함
* 완전히 새로운 요구사항이 생김

필요하면 새로운 Open Question 또는 superseding ADR을 제안한다.

## 작업 절차

코드를 수정하기 전에:

1. 관련 코드와 문서를 읽는다.
2. 현재 동작과 설계 의도를 파악한다.
3. 영향을 받는 invariant와 테스트를 확인한다.

코드를 수정한 뒤에는 가능한 범위에서 관련 테스트를 실행한다.

테스트를 실행하지 않았거나 실행할 수 없었다면 명시한다.

## 성능과 계측

성능 관련 변경은 가능하면 실제 측정 결과를 근거로 한다.

ADR이나 설계 문서에 baseline 값이 있더라도 이를 최적값으로 간주하지 않는다.

재검토 조건이 명시되어 있다면 그 조건이 실제로 충족되었는지 먼저 확인한다.

## 범위 관리

작업 중 별도의 문제를 발견하면 다음과 같이 구분한다.

* 현재 작업의 correctness에 직접 영향을 주는 문제

  * 현재 작업과 함께 보고하고 필요하면 수정
* 현재 작업과 독립적인 문제

  * 별도 관찰 또는 후속 작업 후보로 분리

새로운 설계 문제를 발견했다고 해서 즉시 구현 방향을 넓히지 않는다.

# Codex Instructions

이 파일은 Codex가 자동으로 읽는 저장소 진입점이다. 공통 프로젝트 문맥과 작업 규칙의
단일 출처는 `CLAUDE.md`다.

저장소에서 작업을 시작할 때, 다른 작업을 수행하기 전에 `CLAUDE.md`를 우선 읽어 공통
프로젝트 문맥과 작업 규칙을 확인하고 따른다. 설계와 진행 상태를 추가로 읽는 순서는
그 문서의 `작업 절차` 절을 따르고, 문서를 쓸 때의 규약은 `design/DOC_RULES.md`를
따른다.

공통 규칙을 이 파일에 복제하지 않는다. Codex에만 필요한 진입 지침이 생길 때만 여기에
추가한다.
